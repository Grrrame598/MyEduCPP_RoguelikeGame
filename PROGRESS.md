# Прогресс выполнения домашнего задания

## 📋 Описание проекта

Rogalique - это простой учебный 2д рогалик.
Игрок бегает по случайно сгенерированному лабиринту, убивает монстров и собирает лут.
Сделаем в рамках этого проекта также простой 2д движок.

---

## 📌 Рекомендации по выполнению задания

### ⚠️ Важные принципы архитектуры:

1. **Придерживаться существующей архитектуры**
   - При добавлении классов следовать той архитектуре, которая была выстроена на протяжении модуля
   - Использовать паттерны, уже примененные в проекте (Singleton, Component, и т.д.)

2. **Разделение движка и игры**
   - В движке (`XYZEngine/`) только общий код, который можно переиспользовать
   - В игровом проекте (`XYZRoguelike/`) - конкретная реализация для игры
   - **Плохой пример:** Вписать в движок код воспроизведения музыки по конкретному пути
   - **Хороший пример:** В движке - методы для работы с музыкой, в игре - использование этих методов

3. **Автоматическое размещение стен**
   - Стены и пол не обязательно выставлять вручную
   - Можно написать простой класс, который будет:
     - Расставлять стены по периметру квадрата заданной длины
     - Заполнять пространство полом
   - Позже такой класс можно переделать в генератор уровня

4. **Использование документации**
   - Регулярно обращаться к документации по SFML
   - В документации можно узнать, как работать со звуками и музыкой

5. **Паттерн Стратегия**
   - При любой возможности использовать паттерн Стратегия (см. раздел ниже)

---

## ✅ Критерии выполнения задания

### Обязательные требования:

1. **Музыка в игре**
   - ✅ В игре воспроизводится музыка
   - ✅ При окончании композиции она начинает играть заново (зацикливание)

2. **Враг с преследованием**
   - ✅ Неподалеку (но не в радиусе обнаружения) располагается враг
   - ✅ При приближении игрока враг должен преследовать его
   - ✅ При уходе из радиуса обнаружения враг должен остановиться

3. **Стены, блокирующие движение**
   - ✅ Игрок окружён стенами, которые не дают пройти дальше ему и врагу
   - ✅ При столкновении стены должны оставаться на месте, не реагируя на столкновение
   - ✅ Стены должны быть кинематичными (не двигаются, но блокируют)

### Опциональные требования (по желанию):

4. **Пол на территории**
   - ⏸️ (НЕ ВЫПОЛНЯЕМ) Доступная территория покрыта полом
   - ⏸️ (НЕ ВЫПОЛНЯЕМ) Тайлы пола не должны накладываться друг на друга
   - ⏸️ (НЕ ВЫПОЛНЯЕМ) Они не должны отрисовываться поверх стен

---

## 🎯 Задачи домашнего задания: "Музыка, враг и стены"

### ✅ Шаг 1: Дополнить систему ресурсов возможностью управлять звуками

**Задача:**
- Расширить `ResourceSystem` для работы со звуками и музыкой
- Добавить методы для загрузки `sf::SoundBuffer` (короткие звуки)
- Добавить методы для загрузки `sf::Music` (фоновая музыка)
- По аналогии с текстурами: `LoadSoundBuffer()`, `GetSoundBuffer()`, `DeleteSoundBuffer()`
- По аналогии с текстурами: `LoadMusic()`, `GetMusic()`, `DeleteMusic()`

**Статус:** ✅ Выполнено

**Файлы для изменения:**
- `XYZEngine/ResourceSystem.h` ✅
- `XYZEngine/ResourceSystem.cpp` ✅

**Что сделано:**
- Добавлены методы для `sf::SoundBuffer` (короткие звуки)
- Добавлены методы для `sf::Music` (фоновая музыка)
- Реализованы методы очистки ресурсов
- Обновлен метод `Clear()` для очистки звуков

---

### ✅ Шаг 2: Добавить музыку в игровой проект

**Задача:**
- Добавить фоновую музыку, которая воспроизводится при запуске игры
- Музыка должна зацикливаться (`setLoop(true)`)
- Использовать файл: `Resources/Music/Clinthammer__Background_Music.wav`

**Статус:** ✅ Выполнено

**Файлы для изменения:**
- `XYZRoguelike/GameMain.cpp` (загрузка и запуск музыки)
- Возможно, `XYZRoguelike/DeveloperLevel.cpp` или другой файл инициализации

**Что сделано:**
- Загрузили музыку через `ResourceSystem::LoadMusic`
- Зациклили воспроизведение `setLoop(true)` и запустили `play()`
- Скопировали файл `Clinthammer__Background_Music.wav` в `Resources/Music/`

---

### ✅ Шаг 3: Создать компонент поиска игрока для врага

**Задача:**
- Создать компонент, который будет искать игрока
- Компонент должен задавать цель для приближения (направление движения к игроку)
- Реализация алгоритма поиска и преследования на усмотрение разработчика
- Компонент должен работать с `RigidbodyComponent` для движения врага
- **Враг должен иметь радиус обнаружения** - если игрок в радиусе, враг преследует, если выходит из радиуса - останавливается

**Детали из критериев:**
- Неподалеку (но не в радиусе обнаружения) располагается враг
- При приближении игрока враг должен преследовать его
- При уходе из радиуса обнаружения враг должен остановиться

**Статус:** ✅ Выполнено

**Файлы:**
- `XYZEngine/PlayerChaseComponent.h` (или `EnemyAIComponent.h`)
- `XYZEngine/PlayerChaseComponent.cpp`

**Что сделано:**
- Добавлен `PlayerChaseComponent` (движок): берёт цель, радиус, скорость; если цель в радиусе — задаёт линейную скорость к игроку, иначе останавливается
- Параметры задаются извне: `SetTarget`, `SetDetectionRadius`, `SetSpeed`
- Подготовлено к расширению (можно заменить стратегию позже)

**Идеи для реализации:**
- Использовать `GameWorld` для поиска объекта Player
- Вычислять направление к игроку через `TransformComponent`
- Применять движение через `RigidbodyComponent`

---

### ✅ Шаг 4: Создать класс-контейнер врага

**Задача:**
- Создать класс `Enemy` по аналогии с классом `Player`
- Класс должен быть контейнером для `GameObject` с необходимыми компонентами
- Обязательно добавить компонент поиска игрока (из Шага 3)
- Добавить компоненты: `SpriteRendererComponent`, `TransformComponent`, `RigidbodyComponent`, `SpriteColliderComponent`
- Враг должен иметь текстуру (можно использовать существующую или создать новую)

**Статус:** ✅ Выполнено

**Файлы:**
- `XYZRoguelike/Enemy.h`
- `XYZRoguelike/Enemy.cpp`

**Что сделано:**
- Создан контейнер `Enemy`: добавляет `SpriteRenderer`, `Rigidbody`, `SpriteCollider`, `PlayerChaseComponent`.
- Текстура врага — `platform.png`, размер 64x16 (отличается от игрока).
- Параметры преследования задаются извне; цель — игрок.
- Стартовая позиция врага смещена влево-вверх от игрока.

---

### ✅ Шаг 5: Добавить стены в игру

**Задача:**
- Создать класс-контейнер `Wall` (по аналогии с `Player` и `Enemy`)
- Стены должны окружать область вокруг игрока
- Стены НЕ должны пропускать игрока и врага (физические коллизии)
- Стены должны быть **кинематичными** (использовать `RigidbodyComponent::SetKinematic(true)`)
- Кинематичные объекты не двигаются под воздействием физики, но участвуют в коллизиях

**Статус:** ✅ Выполнено

**Файлы для создания:**
- `XYZRoguelike/Wall.h`
- `XYZRoguelike/Wall.cpp`
- `XYZRoguelike/DeveloperLevel.h/.cpp`

**Что сделано:**
- Добавлен контейнер `Wall`: текстура `platform.png`, коллайдер, кинематичный `Rigidbody` (блокирует игрока и врага).
- В `DeveloperLevel` автоматически строится периметр квадратных стен вокруг игрока (halfSize=640, толщина=16, сегмент 64).
- Стены очищаются при `Stop()`.

---

## 💡 Напоминание о паттернах проектирования

### ⚠️ ВАЖНО: Паттерн Стратегия (Strategy)

**При любой возможности, если таковая появится - использовать паттерн Стратегия!**

Это одна из рекомендаций задания - использовать паттерны проектирования где возможно.

**Где можно применить:**

1. **Разные алгоритмы преследования игрока:**
   - Если нужно реализовать разные типы поведения врага (простое преследование, патрулирование, засада и т.д.)
   - Создать интерфейс `IChaseStrategy` и реализации: `SimpleChaseStrategy`, `PatrolStrategy` и т.д.
   - Компонент `PlayerChaseComponent` использует стратегию

2. **Разные типы движения:**
   - Если появятся разные алгоритмы движения (прямолинейное, по дуге, зигзагом)
   - Интерфейс `IMovementStrategy` с реализациями

3. **Разные алгоритмы генерации/размещения стен:**
   - Если нужно будет разные варианты размещения стен
   - Стратегии генерации уровня

**Текущие паттерны в проекте:**
- ✅ **Синглтон** - используется (Engine, ResourceSystem, RenderSystem, PhysicsSystem, GameWorld)
- ✅ **Компоновщик** - используется (GameObject с children)
- ✅ **Фасад** - используется (Engine как фасад для подсистем)
- ⏳ **Стратегия** - пока не используется, но нужно помнить о возможности применения

---

## 📝 Общие заметки

- Проект использует компонентную архитектуру (ECS-like)
- Все игровые объекты создаются через `GameWorld::Instance()->CreateGameObject()`
- Компоненты добавляются через `gameObject->AddComponent<ComponentType>()`
- Физическая система использует `RigidbodyComponent` и `ColliderComponent`
- Кинематичные объекты (`SetKinematic(true)`) не двигаются, но блокируют движение других объектов

---

## 📌 Чек-лист выполнения

- [x] Шаг 1: Расширить ResourceSystem для звуков и музыки ✅
- [x] Шаг 2: Добавить фоновую музыку с зацикливанием
- [x] Шаг 3: Создать компонент поиска игрока (с радиусом обнаружения)
- [x] Шаг 4: Создать класс Enemy с компонентами
- [x] Шаг 5: Создать класс Wall с кинематичными стенами
- [ ] Проверить работу всех систем
- [ ] Убедиться, что стены блокируют движение игрока и врага
- [ ] Убедиться, что враг преследует игрока при приближении и останавливается при уходе
- [ ] Убедиться, что музыка играет и зацикливается

---

**Дата создания:** 2025-01-06  
**Ветка:** master  
**Статус проекта:** В разработке

